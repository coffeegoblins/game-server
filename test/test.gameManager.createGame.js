var GameManager = require('../gameManager');
var assert = require('assert');
var TestUtility = require('../testUtility');

describe('GameManager', function ()
{
    beforeEach(function () {

    });

    it('Should use a valid source user', function () {

    });

    it('Should use a valid opponent user', function () {

    });

    it('Should use a valid hero type', function () {

    });

    it('Should use a valid level', function () {

    });

    it('Should use valid unit types', function () {

    });

    it('Should not have more units than the level allows', function () {

    });
});
