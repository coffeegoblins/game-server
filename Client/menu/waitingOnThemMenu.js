define(['text!menu/waitingOnThemMenu.html', 'core/src/utility'],
    function (WaitingOnThemTemplate, Utility)
    {
        'use strict';

        function WaitingOnThemMenu(socket)
        {

        }

        WaitingOnThemMenu.prototype.show = function ()
        {

        };

        return WaitingOnThemMenu;
    });
